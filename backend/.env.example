# Backend Environment Variables

# Node environment
NODE_ENV=development

# Server configuration
PORT=3000

# Shiftboard API credentials
SHIFTBOARD_ACCESS_KEY_ID=your-access-key-id
SHIFTBOARD_SECRET_KEY=your-secret-key
SHIFTBOARD_HOST=api.shiftboard.com
SHIFTBOARD_PATH=/api/v1/

# CORS configuration
ALLOWED_ORIGINS=http://localhost:5173

# Committee Configuration (optional)
# Filter API responses to specific workgroups by ID or code
# Leave all empty for global mode (all workgroups)

# Multiple workgroups by ID (comma-separated)
COMMITTEE_IDS=

# Multiple workgroups by code (comma-separated, alternative to IDS)
COMMITTEE_CODES=

# Single workgroup (legacy, use IDS or CODES instead)
COMMITTEE_WORKGROUP=

# Examples:
# IT Committee (filter to ITC, ITC365, ITCS):
#   COMMITTEE_IDS=5676546,5676571,198353
#   OR
#   COMMITTEE_CODES=ITCS,ITC365,ITC
#
# Single Committee Mode (legacy):
#   COMMITTEE_WORKGROUP=safety-team-123
#
# Global Mode (all workgroups - default):
#   Leave COMMITTEE_IDS, COMMITTEE_CODES, and COMMITTEE_WORKGROUP empty

# Logging
LOG_LEVEL=debug

# Mock Data Mode (Development Only)
# Enable mock data generation for UI development/testing when no active shifts exist
# Set to "true" to return realistic mock shifts instead of calling Shiftboard API
ENABLE_MOCK_DATA=false

# Application Insights (Azure - optional for local dev)
# APPLICATIONINSIGHTS_CONNECTION_STRING=your-connection-string
